* Create the dungeons at the real spawn point.
  * Inset the golden block in bigger bridge dungeon
  * Be able to reset bridge dungeon

* BFS in a bigger (but still tiny) bridge problem

* planning with forward search, so it resets after a bad action (closing the loop)

* RMAX after model learning, planning

* orient smoothly 
  * Teleport into dungeons with correct orientation on
